# Common configuration for all boards
# Board-specific settings (BLE, PWM, etc.) are in boards/<board_name>.conf

# USB設定
CONFIG_ZMK_USB=y

# LOG settings (minimal for both boards)
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=2

# ZMK STUDIO settings
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n